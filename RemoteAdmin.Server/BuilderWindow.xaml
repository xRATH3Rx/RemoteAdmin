<Window x:Class="RemoteAdmin.Server.BuilderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Client Builder" Height="550" Width="650"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="DarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SidebarBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="ControlBackground" Color="#333337"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#555555"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#A54ED5"/>
        <SolidColorBrush x:Key="TextColor" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="GrayText" Color="#AAAAAA"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="25"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource GrayText}"/>
            <Setter Property="BorderThickness" Value="0,0,3,0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                </Trigger>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Selected">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentPurple}"/>
                    <Setter Property="Background" Value="#252526"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <StackPanel>
                <Button x:Name="btnBasicSettings" Content="Basic Settings" Style="{StaticResource SidebarButton}" Tag="Selected" Click="SidebarButton_Click"/>
                <Button x:Name="btnConnectionSettings" Content="Connection Settings" Style="{StaticResource SidebarButton}" Click="SidebarButton_Click"/>
                <Button x:Name="btnInstallationSettings" Content="Installation Settings" Style="{StaticResource SidebarButton}" Click="SidebarButton_Click"/>
                <Button x:Name="btnAssemblySettings" Content="Assembly Settings" Style="{StaticResource SidebarButton}" Click="SidebarButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tab Content -->
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="contentArea" Margin="20"/>
            </ScrollViewer>

            <!-- Bottom Button Bar -->
            <Border Grid.Row="1" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Build Client" Width="120" Height="32" Click="BuildClient_Click"
                            Background="{StaticResource AccentPurple}" Foreground="White" 
                            BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Cancel" Width="80" Height="32" Click="Cancel_Click"
                            Background="{StaticResource ControlBackground}" Foreground="White"
                            BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>