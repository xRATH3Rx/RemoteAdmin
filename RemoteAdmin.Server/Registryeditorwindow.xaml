<Window x:Class="RemoteAdmin.Server.RegistryEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registry Editor" 
        Height="600" 
        Width="900"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Modern Dark Theme Resources -->
        <SolidColorBrush x:Key="PrimaryBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#252526"/>
        <SolidColorBrush x:Key="TertiaryBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#3E3E42"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#00FF00"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#FF4444"/>

        <!-- TreeView Style -->
        <Style x:Key="TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AlternatingRowBackground" Value="#252525"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource TertiaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource TertiaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="MinWidth" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Loading Overlay -->
        <Grid x:Name="loadingOverlay" 
              Grid.Row="0" 
              Grid.RowSpan="4" 
              Background="#80000000" 
              Visibility="Collapsed"
              Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Background="{StaticResource TertiaryBackground}">
                <TextBlock Text="⏳" 
                           FontSize="48" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <TextBlock Text="Loading registry data..." 
                           FontSize="16" 
                           Foreground="{StaticResource ForegroundBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource TertiaryBackground}" 
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="15">
            <StackPanel>
                <TextBlock Text="Registry Editor" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="{StaticResource ForegroundBrush}"/>
                <TextBlock x:Name="txtComputerName" 
                           Text="Computer: Loading..." 
                           FontSize="12" 
                           Foreground="#888888"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Navigation Bar -->
        <Border Grid.Row="1" 
                Background="{StaticResource SecondaryBackground}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnBack" 
                        Grid.Column="0"
                        Content="◀" 
                        Style="{StaticResource IconButton}"
                        Click="btnBack_Click"
                        ToolTip="Go Back"
                        Margin="0,0,5,0"/>

                <Button x:Name="btnRefresh" 
                        Grid.Column="1"
                        Content="🔄" 
                        Style="{StaticResource IconButton}"
                        Click="btnRefresh_Click"
                        ToolTip="Refresh"
                        Margin="0,0,10,0"/>

                <TextBox x:Name="txtCurrentPath" 
                         Grid.Column="2"
                         Background="{StaticResource TertiaryBackground}"
                         Foreground="{StaticResource ForegroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1"
                         Padding="10,5"
                         FontSize="13"
                         IsReadOnly="True"
                         VerticalContentAlignment="Center"/>

                <StackPanel Grid.Column="3" 
                            Orientation="Horizontal" 
                            Margin="10,0,0,0">
                    <Button x:Name="btnNewKey" 
                            Content="+ Key" 
                            Style="{StaticResource ModernButton}"
                            Click="btnNewKey_Click"
                            Margin="0,0,5,0"/>
                    <Button x:Name="btnNewValue" 
                            Content="+ Value" 
                            Style="{StaticResource ModernButton}"
                            Click="btnNewValue_Click"
                            Margin="0,0,5,0"/>
                    <Button x:Name="btnDelete" 
                            Content="🗑️ Delete" 
                            Style="{StaticResource ModernButton}"
                            Click="btnDelete_Click"
                            Background="#663333"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- TreeView for Registry Keys -->
            <Border Grid.Column="0" 
                    Background="{StaticResource SecondaryBackground}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <TreeView x:Name="treeRegistry"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="5"
                          ItemContainerStyle="{StaticResource TreeViewItemStyle}"
                          SelectedItemChanged="treeRegistry_SelectedItemChanged">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <TextBlock Text="📁 " FontSize="14"/>
                                <TextBlock Text="{Binding Name}" FontSize="13"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          Width="5" 
                          HorizontalAlignment="Stretch" 
                          Background="{StaticResource BorderBrush}"/>

            <!-- DataGrid for Values -->
            <Border Grid.Column="2" 
                    Background="{StaticResource SecondaryBackground}">
                <DataGrid x:Name="dgValues"
                          MouseDoubleClick="dgValues_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" 
                                          Binding="{Binding Name}" 
                                          Width="*"
                                          MinWidth="150"/>
                        <DataGridTextColumn Header="Type" 
                                          Binding="{Binding Type}" 
                                          Width="120"/>
                        <DataGridTextColumn Header="Data" 
                                          Binding="{Binding Data}" 
                                          Width="2*"
                                          MinWidth="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource TertiaryBackground}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="10,5">
            <TextBlock x:Name="txtStatus" 
                       Text="Ready" 
                       Foreground="{StaticResource ForegroundBrush}"
                       FontSize="12"/>
        </Border>
    </Grid>
</Window>
