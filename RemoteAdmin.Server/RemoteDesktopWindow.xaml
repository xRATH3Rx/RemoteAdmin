<Window x:Class="RemoteAdmin.Server.RemoteDesktopWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remote Desktop" Height="700" Width="1000"
        Background="#1E1E1E"
        Foreground="#F0F0F0"
        Closed="Window_Closed">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryDarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryDarkBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#A54ED5"/>
        <SolidColorBrush x:Key="LightForeground" Color="#F0F0F0"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryDarkBackground}" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="txtClientInfo" 
                               Text="Remote Desktop" 
                               FontWeight="Bold" 
                               FontSize="14"
                               VerticalAlignment="Center"/>

                    <TextBlock x:Name="txtResolution" 
                               Text="" 
                               Margin="20,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Quality:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="cboQuality" 
                              Width="100" 
                              SelectedIndex="1"
                              SelectionChanged="cboQuality_SelectionChanged">
                        <ComboBoxItem Content="Low (50%)"/>
                        <ComboBoxItem Content="Medium (75%)"/>
                        <ComboBoxItem Content="High (90%)"/>
                    </ComboBox>

                    <Button x:Name="btnFullscreen" 
                            Content="Fullscreen" 
                            Width="100" 
                            Margin="10,0,0,0"
                            Click="btnFullscreen_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Desktop View -->
        <Border Grid.Row="1" 
                Background="#000000"
                BorderBrush="{StaticResource SecondaryDarkBackground}"
                BorderThickness="1">
            <ScrollViewer x:Name="DesktopScroller" 
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Image x:Name="imgDesktop" 
                       Stretch="None"
                       MouseDown="imgDesktop_MouseDown"
                       MouseUp="imgDesktop_MouseUp"
                       MouseMove="imgDesktop_MouseMove"
                       MouseWheel="imgDesktop_MouseWheel"/>
            </ScrollViewer>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryDarkBackground}" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtStatus" 
                           Text="Status: Connecting..." 
                           Foreground="#AAAAAA"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="txtFPS" 
                           Grid.Column="1"
                           Text="FPS: 0" 
                           Foreground="#AAAAAA"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>