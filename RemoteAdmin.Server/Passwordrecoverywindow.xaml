<Window x:Class="RemoteAdmin.Server.Passwordrecoverywindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Password Recovery"
        Height="445"
        Width="589"
        MinHeight="400"
        MinWidth="589"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        Foreground="#F0F0F0">

    <Window.Resources>
        <!-- Dark Theme Brushes (matching your other windows) -->
        <SolidColorBrush x:Key="PrimaryDarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryDarkBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="ControlBackground" Color="#333337"/>
        <SolidColorBrush x:Key="ControlBorder" Color="#555555"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#A54ED5"/>
        <SolidColorBrush x:Key="AccentPurpleHover" Color="#B876E8"/>
        <SolidColorBrush x:Key="LightForeground" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="GrayText" Color="#AAAAAA"/>

        <!-- GroupBox style to blend header with dark theme -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="Background" Value="{StaticResource SecondaryDarkBackground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Background="{StaticResource SecondaryDarkBackground}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Grid.Row="1"/>
                            <Border Background="{StaticResource SecondaryDarkBackground}"
                                    BorderBrush="{StaticResource ControlBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="8"
                                    Grid.Row="1">
                                <ContentPresenter />
                            </Border>
                            <Border Background="{StaticResource SecondaryDarkBackground}"
                                    Padding="6,0"
                                    HorizontalAlignment="Left"
                                    Margin="10,-10,0,0">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  Margin="0"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style (accent purple, rounded, hover/press states) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentPurple}"/>
            <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentPurpleHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#8A3DC0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- ListView / GridView for dark mode -->
        <Style TargetType="{x:Type ListView}">
            <Setter Property="Background" Value="{StaticResource SecondaryDarkBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
        </Style>

        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentPurple}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>

        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="Background" Value="{StaticResource SecondaryDarkBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3A2F4A"/>
                    <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2E2B35"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Context Menu + MenuItem (matching your MainWindow style) -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource LightForeground}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="1" Margin="6,2,6,2" VerticalAlignment="Center" />

                                <Path x:Name="Arrow" Grid.Column="2" Margin="6,0,6,0"
                                      VerticalAlignment="Center" Visibility="Collapsed"
                                      Data="M 0 0 L 4 4 L 0 8 Z" Fill="{StaticResource LightForeground}"/>

                                <Popup IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       Placement="Right"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border Background="{StaticResource ControlBackground}"
                                            BorderBrush="{StaticResource ControlBorder}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="2">
                                        <StackPanel IsItemsHost="True"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource GrayText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The actual context menu used by the list -->
        <ContextMenu x:Key="PasswordContextMenu">
            <MenuItem Header="Save to File">
                <MenuItem Header="All" Click="SaveAll_Click"/>
                <MenuItem Header="Selected" Click="SaveSelected_Click"/>
            </MenuItem>

            <MenuItem Header="Copy to Clipboard">
                <MenuItem Header="All" Click="CopyAll_Click"/>
                <MenuItem Header="Selected" Click="CopySelected_Click"/>
            </MenuItem>

            <Separator/>

            <MenuItem Header="Clear">
                <MenuItem Header="All" Click="ClearAll_Click"/>
                <MenuItem Header="Selected" Click="ClearSelected_Click"/>
            </MenuItem>

            <MenuItem Header="Refresh" Click="Refresh_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Recovered Accounts -->
        <GroupBox Grid.Row="0" Header="Recovered Accounts" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ListView x:Name="lstPasswords"
                          Grid.Row="0"
                          Background="{StaticResource SecondaryDarkBackground}"
                          Foreground="{StaticResource LightForeground}"
                          BorderBrush="{StaticResource ControlBorder}"
                          BorderThickness="1"
                          ContextMenu="{StaticResource PasswordContextMenu}"
                          SelectionMode="Extended"
                          GridViewColumnHeader.Click="ListView_ColumnHeaderClick">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Identification" Width="120" DisplayMemberBinding="{Binding Identification}"/>
                            <GridViewColumn Header="URL / Location" Width="170" DisplayMemberBinding="{Binding Url}"/>
                            <GridViewColumn Header="Username" Width="150" DisplayMemberBinding="{Binding Username}"/>
                            <GridViewColumn Header="Password" Width="140" DisplayMemberBinding="{Binding Password}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </GroupBox>

        <!-- Custom Format -->
        <GroupBox Grid.Row="1" Header="Custom Saving/Copying Format" Height="100">
            <Grid Background="{StaticResource SecondaryDarkBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="txtFormat"
                         Grid.Row="0"
                         Text="APP - URL - USER:PASS"
                         HorizontalContentAlignment="Center"
                         Margin="0,6,0,8"
                         Padding="6"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
